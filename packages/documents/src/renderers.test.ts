import { describe, expect, it } from "vitest";
import { createDocumentRendererRegistry, renderDocument, type DocumentRenderer } from "./renderers";
const document={metadata:{documentType:"report",title:"Report",generatedAt:"2026-09-24T12:00:00.000Z",sourceCapability:"CAP.REPORT.READ@1",resourceScope:"report:r1",organizationId:"org_1",workspaceId:"ws_1",actorId:"user_1"},locale:"en-US",timezone:"UTC",calendar:"gregorian" as const,template:{id:"report.default",version:1},header:[],sections:[],footer:[],attachments:[],provenance:{},integrity:{snapshotHash:"a".repeat(64)}};
const renderer:DocumentRenderer={rendererId:"test.pdf",version:1,format:"pdf",async render(doc){return{format:"pdf",mediaType:"application/pdf",filename:"report.pdf",bytes:new Uint8Array([1,2,3]),sourceSnapshotHash:doc.integrity.snapshotHash,rendererId:"test.pdf",rendererVersion:1};}};
describe("document renderers",()=>{it("uses registered renderer for requested format",async()=>{const r=await renderDocument(document,{id:"default",version:1,locale:"en-US",timezone:"UTC",format:"pdf"},createDocumentRendererRegistry([renderer]));expect(r.sourceSnapshotHash).toBe(document.integrity.snapshotHash);});it("fails closed without renderer",async()=>{await expect(renderDocument(document,{id:"default",version:1,locale:"en-US",timezone:"UTC",format:"print"},createDocumentRendererRegistry([renderer]))).rejects.toThrow("No renderer");});});
